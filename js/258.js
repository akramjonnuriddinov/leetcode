const addDigits = function(num) {
  let count = 0;
  if(num <= 9) {
    return 0;
  } else {
    while(num <= 9) {
      // if()
    }
  }
};

const num = 38;
res = addDigits(num);

console.log(res);